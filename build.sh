#!/bin/bash
# This build script will build the project and create the docker image needed for deployment
# Upon failure of compiling code this will return 1, upon failure of docker image build return 2, upon other failure return 3

#TODO: compile and build project here such that it can be automatically run by github actions.

#for now this will return 0 because nothing can be built as of yet.
exit 0